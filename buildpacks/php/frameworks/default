#!/bin/bash

basedir="$( cd -P "$( dirname "$0" )" && pwd )"

case "$1" in
detect)
    exit 1
    ;;
compile)
    echo "-----> Setting up default configuration"
    cp "$basedir/../conf/nginx/default_site.conf.erb" "/app/conf/site.conf.erb"
    ;;
esac
